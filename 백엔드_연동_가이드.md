# 백엔드 로그인 연동 가이드

## 1. 백엔드 API 설계

### 1.1 로그인 API 엔드포인트
```
POST /api/auth/login
Content-Type: application/json

Request Body:
{
  "email": "user@example.com",
  "password": "password123"
}

Response (Success - 200):
{
  "success": true,
  "data": {
    "user": {
      "userId": 1,
      "email": "user@example.com",
      "userName": "홍길동",
      "phone": "010-1234-5678",
      "birthDate": "1990-01-01",
      "stampPoints": 100,
      "status": "active"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "refresh_token_here"
  }
}

Response (Error - 401):
{
  "success": false,
  "error": {
    "code": "INVALID_CREDENTIALS",
    "message": "이메일 또는 비밀번호가 올바르지 않습니다."
  }
}
```

### 1.2 사용자 정보 조회 API
```
GET /api/user/me
Authorization: Bearer {token}

Response (Success - 200):
{
  "success": true,
  "data": {
    "userId": 1,
    "email": "user@example.com",
    "userName": "홍길동",
    "phone": "010-1234-5678",
    "birthDate": "1990-01-01",
    "stampPoints": 100,
    "status": "active"
  }
}
```

### 1.3 토큰 갱신 API
```
POST /api/auth/refresh
Content-Type: application/json

Request Body:
{
  "refreshToken": "refresh_token_here"
}

Response (Success - 200):
{
  "success": true,
  "data": {
    "token": "new_access_token",
    "refreshToken": "new_refresh_token"
  }
}
```

---

## 2. Flutter 앱 설정

### 2.1 필요한 패키지 확인
`pubspec.yaml`에 다음 패키지가 있는지 확인:
```yaml
dependencies:
  http: ^1.1.0  # 이미 있음
  shared_preferences: ^2.2.2  # 이미 있음
  flutter_secure_storage: ^9.0.0  # 토큰 저장용 (추가 필요)
```

### 2.2 패키지 설치
```bash
flutter pub add flutter_secure_storage
```

---

## 3. API 서비스 클래스 생성

### 3.1 `lib/services/auth_api_service.dart` 생성
```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

class AuthApiService {
  // 백엔드 API 베이스 URL (실제 서버 주소로 변경)
  static const String baseUrl = 'https://your-api-server.com/api';
  
  final FlutterSecureStorage _secureStorage = const FlutterSecureStorage();
  
  // 로그인
  Future<Map<String, dynamic>> login(String email, String password) async {
    try {
      final response = await http.post(
        Uri.parse('$baseUrl/auth/login'),
        headers: {
          'Content-Type': 'application/json',
        },
        body: jsonEncode({
          'email': email,
          'password': password,
        }),
      );

      final data = jsonDecode(response.body);
      
      if (response.statusCode == 200 && data['success'] == true) {
        // 토큰 저장
        await _secureStorage.write(
          key: 'access_token',
          value: data['data']['token'],
        );
        await _secureStorage.write(
          key: 'refresh_token',
          value: data['data']['refreshToken'],
        );
        
        return {
          'success': true,
          'user': data['data']['user'],
        };
      } else {
        return {
          'success': false,
          'error': data['error']['message'] ?? '로그인에 실패했습니다.',
        };
      }
    } catch (e) {
      return {
        'success': false,
        'error': '네트워크 오류가 발생했습니다: $e',
      };
    }
  }
  
  // 토큰 가져오기
  Future<String?> getAccessToken() async {
    return await _secureStorage.read(key: 'access_token');
  }
  
  // 사용자 정보 조회
  Future<Map<String, dynamic>> getUserInfo() async {
    try {
      final token = await getAccessToken();
      if (token == null) {
        return {
          'success': false,
          'error': '로그인이 필요합니다.',
        };
      }
      
      final response = await http.get(
        Uri.parse('$baseUrl/user/me'),
        headers: {
          'Authorization': 'Bearer $token',
          'Content-Type': 'application/json',
        },
      );
      
      final data = jsonDecode(response.body);
      
      if (response.statusCode == 200 && data['success'] == true) {
        return {
          'success': true,
          'user': data['data'],
        };
      } else {
        return {
          'success': false,
          'error': data['error']['message'] ?? '사용자 정보를 가져올 수 없습니다.',
        };
      }
    } catch (e) {
      return {
        'success': false,
        'error': '네트워크 오류가 발생했습니다: $e',
      };
    }
  }
  
  // 로그아웃
  Future<void> logout() async {
    await _secureStorage.delete(key: 'access_token');
    await _secureStorage.delete(key: 'refresh_token');
  }
  
  // 토큰 갱신
  Future<bool> refreshToken() async {
    try {
      final refreshToken = await _secureStorage.read(key: 'refresh_token');
      if (refreshToken == null) return false;
      
      final response = await http.post(
        Uri.parse('$baseUrl/auth/refresh'),
        headers: {
          'Content-Type': 'application/json',
        },
        body: jsonEncode({
          'refreshToken': refreshToken,
        }),
      );
      
      final data = jsonDecode(response.body);
      
      if (response.statusCode == 200 && data['success'] == true) {
        await _secureStorage.write(
          key: 'access_token',
          value: data['data']['token'],
        );
        await _secureStorage.write(
          key: 'refresh_token',
          value: data['data']['refreshToken'],
        );
        return true;
      }
      return false;
    } catch (e) {
      return false;
    }
  }
}
```

---

## 4. UserProvider 수정

### 4.1 `lib/providers/user_provider.dart` 수정
```dart
import 'package:flutter/foundation.dart';
import 'package:shared_preferences/shared_preferences.dart';
import '../models/user.dart';
import '../services/auth_api_service.dart';  // 추가

class UserProvider with ChangeNotifier {
  User? _currentUser;
  bool _isLoggedIn = false;
  final AuthApiService _authService = AuthApiService();  // 추가

  // ... 기존 getter들 ...

  Future<Map<String, dynamic>> login(String email, String password) async {
    try {
      // API 호출
      final result = await _authService.login(email, password);
      
      if (result['success'] == true) {
        final userData = result['user'];
        
        _currentUser = User(
          userId: userData['userId'],
          email: userData['email'],
          userName: userData['userName'],
          phone: userData['phone'],
          birthDate: DateTime.parse(userData['birthDate']),
          createdAt: DateTime.now(),
          updatedAt: DateTime.now(),
          status: userData['status'] ?? 'active',
          stampPoints: userData['stampPoints'] ?? 0,
        );
        _isLoggedIn = true;
        
        // SharedPreferences에 로그인 상태 저장
        final prefs = await SharedPreferences.getInstance();
        await prefs.setBool('isLoggedIn', true);
        await prefs.setString('email', email);
        await prefs.setInt('userId', _currentUser!.userId);
        await prefs.setString('userName', _currentUser!.userName);
        await prefs.setString('phone', _currentUser!.phone ?? '');
        await prefs.setInt('stampPoints', _currentUser!.stampPoints);
        
        notifyListeners();
        
        return {
          'success': true,
        };
      } else {
        return {
          'success': false,
          'error': result['error'] ?? '로그인에 실패했습니다.',
        };
      }
    } catch (e) {
      return {
        'success': false,
        'error': '로그인 중 오류가 발생했습니다: $e',
      };
    }
  }

  Future<void> logout() async {
    await _authService.logout();
    
    _currentUser = null;
    _isLoggedIn = false;
    
    // SharedPreferences에서 로그인 상태 제거
    final prefs = await SharedPreferences.getInstance();
    await prefs.remove('isLoggedIn');
    await prefs.remove('email');
    await prefs.remove('userId');
    await prefs.remove('userName');
    await prefs.remove('phone');
    await prefs.remove('stampPoints');
    
    notifyListeners();
  }

  Future<void> checkLoginStatus() async {
    final prefs = await SharedPreferences.getInstance();
    final isLoggedIn = prefs.getBool('isLoggedIn') ?? false;
    
    if (isLoggedIn) {
      // 토큰이 있으면 서버에서 최신 사용자 정보 가져오기
      final result = await _authService.getUserInfo();
      
      if (result['success'] == true) {
        final userData = result['user'];
        
        _currentUser = User(
          userId: userData['userId'],
          email: userData['email'],
          userName: userData['userName'],
          phone: userData['phone'],
          birthDate: DateTime.parse(userData['birthDate']),
          createdAt: DateTime.now(),
          updatedAt: DateTime.now(),
          status: userData['status'] ?? 'active',
          stampPoints: userData['stampPoints'] ?? 0,
        );
        _isLoggedIn = true;
      } else {
        // 토큰이 유효하지 않으면 로그아웃 처리
        await logout();
      }
    }
    
    notifyListeners();
  }
  
  // ... 나머지 메서드들 ...
}
```

---

## 5. LoginScreen 수정

### 5.1 `lib/screens/login_screen.dart` 수정
```dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../widgets/aerok_logo.dart';
import '../theme/aerok_colors.dart';
import '../providers/user_provider.dart';  // 추가

class LoginScreen extends StatefulWidget {
  const LoginScreen({super.key});

  @override
  State<LoginScreen> createState() => _LoginScreenState();
}

class _LoginScreenState extends State<LoginScreen> {
  final _emailController = TextEditingController();
  final _passwordController = TextEditingController();
  final _passwordConfirmController = TextEditingController();  // 회원가입용
  bool _isLogin = true;
  bool _isLoading = false;  // 로딩 상태 추가

  @override
  void dispose() {
    _emailController.dispose();
    _passwordController.dispose();
    _passwordConfirmController.dispose();
    super.dispose();
  }

  Future<void> _handleLogin() async {
    if (_emailController.text.isEmpty || _passwordController.text.isEmpty) {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(
          content: Text('이메일과 비밀번호를 입력해주세요.'),
          backgroundColor: Colors.red,
        ),
      );
      return;
    }

    setState(() {
      _isLoading = true;
    });

    try {
      final userProvider = context.read<UserProvider>();
      final result = await userProvider.login(
        _emailController.text.trim(),
        _passwordController.text,
      );

      if (!mounted) return;

      if (result['success'] == true) {
        Navigator.pop(context);
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(
            content: Text('로그인 성공!'),
            backgroundColor: Colors.green,
          ),
        );
      } else {
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(
            content: Text(result['error'] ?? '로그인에 실패했습니다.'),
            backgroundColor: Colors.red,
          ),
        );
      }
    } catch (e) {
      if (!mounted) return;
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text('오류가 발생했습니다: $e'),
          backgroundColor: Colors.red,
        ),
      );
    } finally {
      if (mounted) {
        setState(() {
          _isLoading = false;
        });
      }
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(_isLogin ? '로그인' : '회원가입'),
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(24),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.stretch,
          children: [
            const SizedBox(height: 40),
            const AeroKLogo(
              width: 180,
              height: 38,
              useDarkText: true,
            ),
            const SizedBox(height: 48),
            TextField(
              controller: _emailController,
              decoration: InputDecoration(
                labelText: '이메일',
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
                prefixIcon: const Icon(Icons.email),
              ),
              keyboardType: TextInputType.emailAddress,
            ),
            const SizedBox(height: 16),
            TextField(
              controller: _passwordController,
              obscureText: true,
              decoration: InputDecoration(
                labelText: '비밀번호',
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(12),
                ),
                prefixIcon: const Icon(Icons.lock),
              ),
            ),
            if (!_isLogin) ...[
              const SizedBox(height: 16),
              TextField(
                controller: _passwordConfirmController,
                obscureText: true,
                decoration: InputDecoration(
                  labelText: '비밀번호 확인',
                  border: OutlineInputBorder(
                    borderRadius: BorderRadius.circular(12),
                  ),
                  prefixIcon: const Icon(Icons.lock_outline),
                ),
              ),
            ],
            const SizedBox(height: 32),
            Container(
              decoration: BoxDecoration(
                gradient: AeroKColors.primaryGradient,
                borderRadius: BorderRadius.circular(12),
                boxShadow: [
                  BoxShadow(
                    color: AeroKColors.darkNavy.withOpacity(0.3),
                    blurRadius: 8,
                    offset: const Offset(0, 4),
                  ),
                ],
              ),
              child: ElevatedButton(
                onPressed: _isLoading ? null : (_isLogin ? _handleLogin : null),  // 회원가입은 나중에 구현
                style: ElevatedButton.styleFrom(
                  backgroundColor: Colors.transparent,
                  shadowColor: Colors.transparent,
                  padding: const EdgeInsets.symmetric(vertical: 16),
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(12),
                  ),
                ),
                child: _isLoading
                    ? const SizedBox(
                        height: 20,
                        width: 20,
                        child: CircularProgressIndicator(
                          strokeWidth: 2,
                          valueColor: AlwaysStoppedAnimation<Color>(Colors.white),
                        ),
                      )
                    : Text(
                        _isLogin ? '로그인' : '회원가입',
                        style: const TextStyle(
                          fontSize: 16,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
              ),
            ),
            const SizedBox(height: 16),
            TextButton(
              onPressed: () {
                setState(() {
                  _isLogin = !_isLogin;
                });
              },
              child: Text(
                _isLogin
                    ? '계정이 없으신가요? 회원가입'
                    : '이미 계정이 있으신가요? 로그인',
                style: const TextStyle(color: AeroKColors.darkNavy),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

---

## 6. HTTP 인터셉터 설정 (선택사항)

### 6.1 `lib/services/http_interceptor.dart` 생성
토큰 자동 추가 및 갱신을 위한 인터셉터:
```dart
import 'package:http/http.dart' as http;
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

class HttpInterceptor {
  static const FlutterSecureStorage _storage = FlutterSecureStorage();
  static const String baseUrl = 'https://your-api-server.com/api';
  
  static Future<http.Response> get(String endpoint) async {
    final token = await _storage.read(key: 'access_token');
    return await http.get(
      Uri.parse('$baseUrl$endpoint'),
      headers: {
        'Authorization': 'Bearer $token',
        'Content-Type': 'application/json',
      },
    );
  }
  
  static Future<http.Response> post(
    String endpoint,
    Map<String, dynamic> body,
  ) async {
    final token = await _storage.read(key: 'access_token');
    return await http.post(
      Uri.parse('$baseUrl$endpoint'),
      headers: {
        'Authorization': 'Bearer $token',
        'Content-Type': 'application/json',
      },
      body: jsonEncode(body),
    );
  }
}
```

---

## 7. 환경 변수 설정

### 7.1 `lib/config/api_config.dart` 생성
```dart
class ApiConfig {
  // 개발 환경
  static const String devBaseUrl = 'http://localhost:3000/api';
  
  // 운영 환경
  static const String prodBaseUrl = 'https://your-api-server.com/api';
  
  // 현재 환경 (환경 변수로 관리)
  static const String baseUrl = devBaseUrl;  // 또는 prodBaseUrl
}
```

---

## 8. 테스트 체크리스트

- [ ] 백엔드 API 서버가 실행 중인지 확인
- [ ] API 엔드포인트가 올바른지 확인
- [ ] CORS 설정 확인 (웹의 경우)
- [ ] SSL 인증서 확인 (HTTPS)
- [ ] 네트워크 권한 확인 (Android: INTERNET, iOS: App Transport Security)
- [ ] 토큰 저장/조회 테스트
- [ ] 로그인 성공/실패 케이스 테스트
- [ ] 토큰 만료 시 갱신 테스트

---

## 9. 보안 고려사항

1. **토큰 저장**: `flutter_secure_storage` 사용 (SharedPreferences 대신)
2. **HTTPS 사용**: 모든 API 통신은 HTTPS로
3. **토큰 만료 처리**: 자동 갱신 로직 구현
4. **에러 처리**: 네트워크 오류, 인증 오류 등 적절히 처리
5. **로딩 상태**: 사용자에게 로딩 상태 표시

---

## 10. 다음 단계

1. 회원가입 API 연동
2. 비밀번호 재설정 API 연동
3. 프로필 수정 API 연동
4. 스탬프 정보 동기화
5. 예약 정보 동기화

